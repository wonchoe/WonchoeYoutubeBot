# 🍪 Як оновити YouTube Cookies (коли вони застаріли)

## Проблема
```
WARNING: [youtube] The provided YouTube account cookies are no longer valid. 
They have likely been rotated in the browser as a security measure.
```

YouTube автоматично ротує cookies кожні кілька днів/тижнів. Потрібно експортувати нові.

---

## ✅ МЕТОД 1: Browser Extension (НАЙПРОСТІШИЙ)

### 1. Встановіть розширення
- Chrome/Edge: [Get cookies.txt LOCALLY](https://chrome.google.com/webstore/detail/get-cookiestxt-locally/cclelndahbckbenkjhflpdbgdldlbecc)
- Firefox: [cookies.txt](https://addons.mozilla.org/en-US/firefox/addon/cookies-txt/)

### 2. Експортуйте cookies
1. Відкрийте **YouTube.com** в браузері
2. **Залогіньтесь** в акаунт YouTube
3. Клікніть на іконку розширення
4. Виберіть "Export" → "Current Site" (тільки youtube.com)
5. Збережіть як `youtube_cookies.txt`

### 3. Завантажте на сервер
```bash
# Замініть cookies на K3s сервері
scp youtube_cookies.txt your-server:/var/www/ytdl-cookies.txt

# Рестарт бота
kubectl rollout restart deployment/ytdl-bot -n wonchoeyoutubebot

# Перевірте логи
kubectl logs -f deployment/ytdl-bot -n wonchoeyoutubebot
```

---

## ✅ МЕТОД 2: yt-dlp CLI (АВТОМАТИЧНИЙ)

**На машині з браузером Chrome:**
```bash
# Встановіть yt-dlp
pip install -U yt-dlp

# Експортуйте cookies прямо з браузера
yt-dlp --cookies-from-browser chrome --cookies youtube_fresh.txt https://www.youtube.com

# Завантажте на сервер
scp youtube_fresh.txt your-server:/var/www/ytdl-cookies.txt
```

**Firefox:**
```bash
yt-dlp --cookies-from-browser firefox --cookies youtube_fresh.txt https://www.youtube.com
```

---

## ✅ МЕТОД 3: DevTools (РУЧНИЙ)

### 1. Відкрийте YouTube
1. Зайдіть на https://www.youtube.com (ОБОВ'ЯЗКОВО ЗАЛОГІНЕНІ!)
2. F12 → Console tab

### 2. Запустіть скрипт
```javascript
// Експорт всіх YouTube cookies
var cookies = document.cookie.split(';');
var result = '# Netscape HTTP Cookie File\n';
result += '# Generated by DevTools\n';

cookies.forEach(function(cookie) {
    var parts = cookie.trim().split('=');
    var name = parts[0];
    var value = parts.slice(1).join('=');
    result += '.youtube.com\tTRUE\t/\tTRUE\t9999999999\t' + name + '\t' + value + '\n';
});

console.log(result);
```

### 3. Скопіюйте вивід
- Збережіть в файл `youtube_cookies.txt`
- Завантажте на сервер:
```bash
scp youtube_cookies.txt your-server:/var/www/ytdl-cookies.txt
kubectl rollout restart deployment/ytdl-bot -n wonchoeyoutubebot
```

---

## 🔍 Перевірка

Після рестарту в логах має бути:
```
✅ Fixed 30+ cookies
✅ Node.js detected: v20.19.6
✅ Critical cookies found: __Secure-3PSID, __Secure-1PSID, SAPISID, SSID
```

**БЕЗ WARNING:**
```
WARNING: [youtube] The provided YouTube account cookies are no longer valid
```

---

## ⚠️ Важливо

1. **Завжди логінтесь** в YouTube перед експортом
2. **Не використовуйте Incognito** режим
3. Cookies діють **декілька днів/тижнів**, потім треба оновлювати
4. **Не ділитесь cookies** - вони дають доступ до вашого акаунту

---

## 🤖 Автоматизація (опціонально)

### Cron на сервері для авто-оновлення
```bash
# Додайте в crontab
0 0 * * 0 yt-dlp --cookies-from-browser chrome --cookies /var/www/ytdl-cookies.txt https://www.youtube.com && kubectl rollout restart deployment/ytdl-bot -n wonchoeyoutubebot
```

**Але:** Потрібно щоб Chrome працював на сервері з активною сесією YouTube.

---

## 📝 Troubleshooting

### "No supported browser found"
```bash
# Вкажіть шлях до профілю Chrome вручну
yt-dlp --cookies-from-browser chrome:/path/to/profile --cookies cookies.txt https://www.youtube.com
```

### "Permission denied"
```bash
# Закрийте Chrome перед експортом
pkill chrome
yt-dlp --cookies-from-browser chrome --cookies cookies.txt https://www.youtube.com
```

### Cookies все ще не працюють
1. Перевірте чи залогінені в YouTube
2. Спробуйте інший браузер (Firefox замість Chrome)
3. Очистіть кеш браузера і залогіньтесь заново
4. Експортуйте cookies одразу після логіну

---

**Дата останнього оновлення:** 2025-12-07
